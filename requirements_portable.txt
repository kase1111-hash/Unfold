# Unfold Portable - Dependencies for PyInstaller Build
# These packages are bundled into the portable executable

# Web Framework (core)
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
starlette>=0.36.0

# Data Validation (required)
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# Database (SQLite + SQLAlchemy for local storage)
sqlalchemy[asyncio]==2.0.25

# HTTP Client (for Ollama API)
httpx==0.26.0

# JSON (fast serialization)
orjson>=3.9.0

# Async support
anyio>=4.0.0
sniffio>=1.3.0

# Type hints
typing_extensions>=4.0.0
annotated-types>=0.6.0

# Build Tools
pyinstaller==6.3.0

# Note: Heavy dependencies (spacy, langchain, neo4j) are excluded
# to keep the executable size reasonable. The portable build uses
# Ollama for LLM-powered extraction instead.
